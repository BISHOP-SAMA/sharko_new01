{
  "buildCommand": "npm run build",
  "outputDirectory": "dist/public",
  "framework": null,
  "env": {
    "VERCEL_PRERENDER_METADATA_BOUNDARY": "1"
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/:path*" },
    { "source": "/(.*)", "destination": "/" }
  ]
}
